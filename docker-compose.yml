version: '3.8'

services:
  angular-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: soumya261412/angular-app:latest
    ports:
      - "80:80"
    networks:
      - my_network

networks:
  my_network:
    driver: overlay
